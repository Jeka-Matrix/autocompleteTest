export enum SERVICES_ENUM {
  shlihim = 1,
  tevel,
  rashum
}

export enum SERVICES_NAMES_ENUM {
  shlihim = "shlihim",
  tevel = "tevel",
  rashum = "rashum"
}


export enum WeightRangeEnum {
  sm = 1,
  md,
  lg
}

export enum OTP_PREFERENCE {
  sms = 1,
  email = 2
}

export enum SHLIHIM_STATUS_ENUM {
  InProgress = 1,
  NotCollected,
  SentTo,
  ReturnTo
}


export enum USER_PREMISSIONS_ENUM {
  admin = "Admin",
  basic = "Basic"
}



export enum pickupsStatusEnum {
  Completed = 1,
  Scheduled = 2,
  Cancel = 3,
  Rejected = 4,
  Open = 5,
  Accepted
}